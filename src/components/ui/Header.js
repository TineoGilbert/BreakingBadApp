import React from 'react'
import logo from '../../img/logo.png';

export const Header = () => {
    return (
        <headder className='center'>
            <img src={logo} alt=''/>
        </headder>
    )
}

export default Header;
